import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    buttonContainer: {
      flexDirection: 'row',
    },
    button: {
      flex: 1,
      backgroundColor: '#722F37',
      alignItems: 'center',
      paddingVertical: 0.5,
      borderRadius: 10,
      marginHorizontal: 5,
    },
});